version: "3"

includes:
  dokploy:
    taskfile: ./hetzner-dokploy/Taskfile.yml
    dir: ./hetzner-dokploy
  k3s:
    taskfile: ./hetzner-k3s/Taskfile.yml
    dir: ./hetzner-k3s

tasks:
  default:
    desc: List available tasks
    cmds:
      - task --list
